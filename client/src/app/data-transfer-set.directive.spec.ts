import { DataTransferSetDirective } from './data-transfer-set.directive';

describe('DataTransferSetDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTransferSetDirective();
    expect(directive).toBeTruthy();
  });
});
